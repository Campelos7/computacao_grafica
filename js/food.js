/**
 * Ponto de entrada legado: reexporta a classe `Food` e constantes da pasta `food/`.
 * O código de jogo pode continuar a usar `import { Food } from './food.js'`.
 */
export { Food, ITEM_TYPES } from './food/index.js';
